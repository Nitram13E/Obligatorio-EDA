typedef struct Linea
{
	char * contlinea;
	int indice;
	struct Linea * next;
	
}*linea;

typedef struct Version
{
	char * ver;
	linea contenido;
	struct Version * subVersion;
	
}*version;


typedef struct Archivo
{
	char * nombre;
	int largo;
	version versiones;
	
}*archivo;
